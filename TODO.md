# TODO
* Store tabs in a PGLite instance (`main.tsx`, `connection.tsx`, `create-tables.sql`).
* Create a management component to view all tabs (`management.tsx`).
* Money input, add to a tab for a user that you'd define in the button flow (`accept-tab.tsx`).
* Tab -> find tab or create tab -> decline if balance is too high, otherwise add the balance (`accept-tab.tsx`).
